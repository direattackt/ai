#ifndef COMMON

#include <stdio.h>
#include <string.h>

#define MAXCHAR 1000
char str[MAXCHAR];

int count() {printf("count\n"); return 0;}
void syl() {return;}


int loadFile()
{
  printf("loadFile\n");
  char str[MAXCHAR];
  char filename[255] = "th vast ie ay/aeiba otehinta heitna oeitna eobi roaebi aroeb iary.";
  char symbol = 0;

  FILE *fp = fopen(filename, "r");
  if(fp == NULL)
  {
  	printf("Could not open file %s\n", filename);
  	return 0;
  } else {
  	if(fgets(str, MAXCHAR, fp) != NULL) {
  		printf("%s\n", str);

  		for(int c = 0; c <= strlen(str); c++)
  		{
  			printf("%c\n", str[c] );

  		}
  		//get one line at a time
  	}
    fclose(fp);
  }
   
  return 1;
}

int loadsex(bet, key, asiii) asciii
bin 

nt 

int loadb(belly)au tu 
start mic 
start fat 
fast 

cat
dot
eo n 
not

int loadM(char bin)
{
	//iso
	int mat = 0;
	
	//bi ascii
	//ib ni bn 
	auto bc 
	bin    bin :+ 0
	//89
	//89

	7bc bin ut:=0
	gcc auto br 

	abo ericbao enas tns.b arc r. 

	if(bin)
}

int isgoingbelo_aw_co(btt)


/*
void count ty
tyne
tyne
di 

fast
sow
slow
abmebr
melt
gu
gui
guil
sound*/

#endif